{{- range $name, $svc := .Values.services }}
{{- if $svc.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $name }}-config
  namespace: {{ $.Values.global.namespace }}
data:
  ENV: {{ $.Values.global.env | quote }}
  PORT: "8080"
{{- end }}
{{- end }}
